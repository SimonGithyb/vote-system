import Vue from 'vue';
import VoteCreatorComp from './VoteCreatorComp.vue';

// Here are some Jasmine 2.0 tests, though you can
// use any test runner / assertion library combo you prefer
describe('VoteCreatorComp', () => {
  // Inspect the raw component options
  it('has a name', () => {
    expect(VoteCreatorComp.name).toBe('VoteCreatorComp');
  })
})