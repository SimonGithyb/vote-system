import Vue from 'vue';
import VoteCastComp from './VoteCastComp.vue';

// Here are some Jasmine 2.0 tests, though you can
// use any test runner / assertion library combo you prefer
describe('VoteCastComp', () => {
  // Inspect the raw component options
  it('has a name', () => {
    expect(VoteCastComp.name).toBe('VoteCastComp');
  })
})